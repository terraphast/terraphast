#include "leaf_set.h"


UnionFindLeafSet::apply_constraints(const std::vector<constraint> &constraints) {

}

UnionFindLeafSet::contains(size_t leaf) {
    return data_structure->find(leaf) == set;
}

UnionFindLeafSet::get_nth_partition_tuple(std::vector<LeafSet> &partitions, size_t n) {

}

